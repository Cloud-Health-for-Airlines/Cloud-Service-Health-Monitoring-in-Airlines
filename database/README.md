# database/

Data layer for BACCP.

Planned contents:
- `schema/` — graph metadata schema (nodes, edges, generation-type attributes) for PostgreSQL
- `telemetry/` — time-series schema for CloudWatch/X-Ray-derived metrics
- `synthetic-data/` — scripts to generate/seed the synthetic testbed data (since no public real-world airline dataset exists)

Status: not yet implemented — placeholder for Phase-II development.
